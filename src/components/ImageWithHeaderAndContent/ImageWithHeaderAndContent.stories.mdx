import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import { boolean, select } from '@storybook/addon-knobs';
import DefinitionList from '~/components/DefinitionList';
import KitList from '~/components/KitList';
import ImageWithHeaderAndContent from './ImageWithHeaderAndContent';
import ImageWithHeaderAndContentFixture from './ImageWithHeaderAndContent.fixture';

<Meta
  title="Components/ImageWithHeaderAndContent"
  component={ImageWithHeaderAndContent}
  decorators={[withKnobs]}
/>

# ImageWithHeaderAndContent

**Shown as With Definition List**

<Preview>
  <Story name="Base component">
    <ImageWithHeaderAndContent
      content={
        <DefinitionList
          items={ImageWithHeaderAndContentFixture.definitionList}
        />
      }
      copy={ImageWithHeaderAndContentFixture.copy}
      hasFullWidthImage={boolean('hasFullWidthImage', false)}
      image={ImageWithHeaderAndContentFixture.imageExpectations}
      isReverse={boolean('reverse', false)}
      theme={select('theme', ['1', '2', '3', '4', '5'], '1')}
    />
    <ImageWithHeaderAndContent
      content={
        <DefinitionList
          items={ImageWithHeaderAndContentFixture.definitionList}
        />
      }
      copy={ImageWithHeaderAndContentFixture.copy}
      hasFullWidthImage={boolean('hasFullWidthImage', false)}
      image={ImageWithHeaderAndContentFixture.imageExpectations}
      isReverse={boolean('reverse', false)}
      theme={select('theme', ['1', '2', '3', '4', '5'], '1')}
    />
    <ImageWithHeaderAndContent
      content={
        <DefinitionList
          items={ImageWithHeaderAndContentFixture.definitionList}
        />
      }
      copy={ImageWithHeaderAndContentFixture.copy}
      hasFullWidthImage={boolean('hasFullWidthImage', false)}
      image={ImageWithHeaderAndContentFixture.imageExpectations}
      isReverse={boolean('reverse', false)}
      theme={select('theme', ['1', '2', '3', '4', '5'], '1')}
    />
    <ImageWithHeaderAndContent
      content={
        <DefinitionList
          items={ImageWithHeaderAndContentFixture.definitionList}
        />
      }
      copy={ImageWithHeaderAndContentFixture.copy}
      hasFullWidthImage={boolean('hasFullWidthImage', false)}
      image={ImageWithHeaderAndContentFixture.imageExpectations}
      isReverse={boolean('reverse', false)}
      theme={select('theme', ['1', '2', '3', '4', '5'], '1')}
    />
  </Story>
</Preview>

## API

<Props of={ImageWithHeaderAndContent} />

## Usage Example

```
import ImageWithHeaderAndContent from '@aesop-gel/components/ImageWithHeaderAndContent'

...

<ImageWithHeaderAndContent
  content={}
  copy={}
  hasFullWidthImage={}
  image={}
  isReverse={false}
  theme={}
/>
```

### With Kit List

<Preview>
  <Story name="With Kit List">
    <ImageWithHeaderAndContent
      content={<KitList items={ImageWithHeaderAndContentFixture.kitList} />}
      copy={ImageWithHeaderAndContentFixture.copyKit}
      image={ImageWithHeaderAndContentFixture.imageKit}
    />
  </Story>
</Preview>

### With Expectations

<Preview>
  <Story name="With Expectations">
    <ImageWithHeaderAndContent
      copy={ImageWithHeaderAndContentFixture.copyExpectations}
      image={ImageWithHeaderAndContentFixture.imageExpectations}
      isReverse={true}
    />
  </Story>
</Preview>
