import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { select } from '@storybook/addon-knobs';
import TextOverFullWidthAsset from './TextOverFullWidthAsset';
import TextOverFullWidthAssetFixture from './TextOverFullWidthAsset.fixture';

<Meta title="Components/TextOverFullWidthAsset" component={TextOverFullWidthAsset} />

# TextOverFullWidthAsset

<Preview>
  <Story name="Base component">
    <TextOverFullWidthAsset
      backgroundImage={TextOverFullWidthAssetFixture.backgroundImage}
      backgroundVideo={TextOverFullWidthAssetFixture.backgroundVideo}
      content={<p>Hello word</p>}
      copyHeight={select('Content vertical location', ['Top', 'Bottom'], 'Top', 'Layout')}
      copySide={select('Content horizontal location', ['Left', 'Right'], 'Left', 'Layout')}
      mediaType={select('Media type', ['Image', 'Video'], 'Image', 'Presentation')}
    />
  </Story>
</Preview>

## API

<Props of={TextOverFullWidthAsset} />
