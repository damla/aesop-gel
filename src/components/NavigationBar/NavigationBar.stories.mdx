import { Meta, Story, Preview, ArgsTable } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import NavigationBar from './NavigationBar';
import navigationBarFixture from './NavigationBar.fixture';

<Meta title="Components/NavigationBar" component={NavigationBar} />

# NavigationBar

<Preview>
  <Story name="Base component">
    <NavigationBar 
      parentLink={navigationBarFixture.parentLink}
      childLinks={navigationBarFixture.childLinks}
    />
  </Story>
</Preview>

## Variations

### With a selected link

<Preview>
  <Story name="With a selected link">
    <NavigationBar 
      parentLink={navigationBarFixture.parentLink}
      childLinks={navigationBarFixture.childLinks}
      selectedUrl={navigationBarFixture.selectedUrl}
    />
  </Story>
</Preview>

### Without a parent link

<Preview>
  <Story name="Without a parent link">
    <NavigationBar 
      childLinks={navigationBarFixture.childLinks}
    />
  </Story>
</Preview>

### Without child links

<Preview>
  <Story name="Without child links">
    <NavigationBar 
      parentLink={navigationBarFixture.parentLink}
    />
  </Story>
</Preview>

### Without any links

<Preview>
  <Story name="Without any links">
    <NavigationBar />
  </Story>
</Preview>

## API

<ArgsTable of={NavigationBar} />
