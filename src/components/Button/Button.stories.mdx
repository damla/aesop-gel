import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import * as knobs from '@storybook/addon-knobs';
import Button from './Button';

<Meta title="Components/Button" component={Button} />

# Button

<Preview>
  <Story name="Base component">
    <Button
      isAlternate={knobs.boolean('isAlternate', false, 'Presentation')}
      isEnabled={knobs.boolean('isEnabled', true, 'Presentation')}
      isInline={knobs.boolean('isInline', false, 'Presentation')}
      onClick={action('clicked')}
      theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
      title={knobs.text('title', 'Aēsop', 'Copy')}
    >
      {knobs.text('children', 'Button')}
    </Button>
  </Story>
</Preview>

## API

<Props of={Button} />
