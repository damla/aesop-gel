import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Hyperlink from '~/components/Hyperlink';
import LinkButtonGroup from './LinkButtonGroup';
import LinkButtonGroupFixture from './LinkButtonGroup.fixture';

<Meta title="Components/LinkButtonGroup" component={LinkButtonGroup} />

# LinkButtonGroup

<Preview>
  <Story name="Base component">
    <LinkButtonGroup
      hasFitContent={knobs.boolean('hasFitContent', false, 'Presentation')}
      theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
      textAlign={knobs.select(
        'textAlign',
        ['center', 'left', 'right'],
        'center',
        'Presentation',
      )}
    >
      <Hyperlink
        url={knobs.text('url', LinkButtonGroupFixture.link.url, 'First link')}
        style={knobs.select(
          'style',
          [
            'External Button Link',
            'Internal Button Link',
            'External No Icon Button Link',
            'Internal No Icon Button Link',
          ],
          LinkButtonGroupFixture.link.style,
          'First link',
        )}
      >
        {knobs.text(
          'children',
          LinkButtonGroupFixture.link.text,
          'First link',
        )}
      </Hyperlink>
      <Hyperlink
        url={knobs.text('url', LinkButtonGroupFixture.secondaryLink.url, 'First link')}
        style={knobs.select(
          'style',
          [
            'External Button Link',
            'Internal Button Link',
            'External No Icon Button Link',
            'Internal No Icon Button Link',
          ],
          LinkButtonGroupFixture.secondaryLink.style,
          'First link',
        )}
      >
        {knobs.text(
          'children',
          LinkButtonGroupFixture.secondaryLink.text,
          'First link',
        )}
      </Hyperlink>
    </LinkButtonGroup>
  </Story>
</Preview>

## API

<Props of={LinkButtonGroup} />
