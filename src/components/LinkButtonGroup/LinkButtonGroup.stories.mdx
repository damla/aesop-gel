import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import { text, select, boolean } from '@storybook/addon-knobs';
import LinkButtonGroup from './LinkButtonGroup';
import LinkButtonGroupFixture from './LinkButtonGroup.fixture';

<Meta
  title="Components/LinkButtonGroup"
  component={LinkButtonGroup}
  decorators={[withKnobs]}
/>

# LinkButtonGroup

<Preview>
  <Story name="Base component">
    <LinkButtonGroup
      hasFitContent={boolean('hasFitContent', false)}
      theme={select('theme', ['dark', 'light'], 'dark')}
      textAlign={select('textAlign', ['center', 'left', 'right'], 'center')}
      link={{
        text: text('link.text', LinkButtonGroupFixture.link.text),
        url: text('link.url', LinkButtonGroupFixture.link.url),
        style: select(
          'link.style',
          [
            'External Button Link',
            'Internal Button Link',
            'External No Icon Button Link',
            'Internal No Icon Button Link',
          ],
          LinkButtonGroupFixture.link.style,
        ),
      }}
      secondaryLink={{
        text: text(
          'secondaryLink.text',
          LinkButtonGroupFixture.secondaryLink.text,
        ),
        url: text(
          'secondaryLink.url',
          LinkButtonGroupFixture.secondaryLink.url,
        ),
        style: select(
          'secondaryLink.style',
          [
            'External Button Link',
            'Internal Button Link',
            'External No Icon Button Link',
            'Internal No Icon Button Link',
          ],
          LinkButtonGroupFixture.secondaryLink.style,
        ),
      }}
    />
  </Story>
</Preview>

## API

<Props of={LinkButtonGroup} />

## Usage Example

```
<LinkButtonGroup />
```
