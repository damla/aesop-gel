import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Podium from '~/components/Podium';
import ContentHubArticle from './ContentHubArticle';
import ContentHubArticleFixture from './ContentHubArticle.fixture';

<Meta title="Components/ContentHubArticle" component={ContentHubArticle} />

# ContentHubArticle

<Preview>
  <Story name="Base component">
    <Podium horizontalPadding="small" verticalPadding="small">
      <ContentHubArticle
        id={ContentHubArticleFixture.id}
        category={knobs.select('category', ['Culture', 'Product', 'The Weekly', 'Aesop Spotify'], 'Culture', 'Content')}
        dataTestRef={ContentHubArticleFixture.dataTestRef}
        handleClickTracking={ContentHubArticleFixture.handleClickTracking}
        handleImpression={ContentHubArticleFixture.handleImpression}
        isHorizontal={knobs.select('isHorizontal', [true, false], true, 'Content')}
        isReadMore={knobs.select('isReadMore', [true, false], false, 'Content')}
        isMenuItem={knobs.select('isMenuItem', [true, false], false, 'Content')}
        horizontalThumbnail={ContentHubArticleFixture.horizontalThumbnail}
        longTitle={knobs.text('title', ContentHubArticleFixture.longTitle, 'Content')}
        readingTime={knobs.text('readingTime', ContentHubArticleFixture.readingTime, 'Content')}
        uri={ContentHubArticleFixture.uri}
        verticalThumbnail={ContentHubArticleFixture.verticalThumbnail}
      />
    </Podium>
  </Story>
</Preview>

## API

<Props of={ContentHubArticle} />
