import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Icon from './Icon';
import svgs from './Icon.svgs';

<Meta title="Components/Icon" component={Icon} />

# Icon

<Preview>
  <Story name="Base component">
    <div style={{ display: 'flex', flexWrap: 'wrap', textAlign: 'center' }}>
      {svgs.map((svg, key) => (
        <div style={{ padding: '20px 40px' }} key={key}>
          <Icon
            height={knobs.number('height', 50, 'Presentation')}
            width={knobs.number('width', 50, 'Presentation')}
            name={svg.name}
            theme={knobs.select(
              'theme',
              ['dark', 'light'],
              'dark',
              'Presentation',
            )}
          />
          <div style={{ marginTop: '10px' }}>{svg.name}</div>
        </div>
      ))}
    </div>
  </Story>
</Preview>

## API

<Props of={Icon} />
