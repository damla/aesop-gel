import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import { select } from '@storybook/addon-knobs';
import Icon from './Icon';
import svgs from './Icon.svgs';

<Meta title="Icon" component={Icon} decorators={[withKnobs]} />

# Icon

<Preview>
  <Story name="Base component">
    <div className="aesop-gel-grid">
      {svgs.map((svg, key) => (
        <div className="aesop-gel-grid--cell" key={key}>
          <Icon name={svg.name} />
        </div>
      ))}
    </div>
  </Story>
</Preview>

## API

<Props of={Icon} />

## Usage Example

```
import Icon from '@aesop-gel/components/Icon'

...

<Icon name={svg_name} />

```
