import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { VariantSelectContextProvider } from '~/contexts';
import ProductDetailHeaderFixture from '~/components/ProductDetailHeader/ProductDetailHeader.fixture';
import ProductDetailImageFixture from '~/components/ProductDetailHeader/components/ProductDetailImage/ProductDetailImage.fixture';
import ProductDetailImage from '~/components/ProductDetailHeader/components/ProductDetailImage';

<Meta
  title="Components/ProductDetailHeader/ProductDetailImage"
  component={ProductDetailImage}
/>

# ProductDetailImage

<Preview>
  <Story name="Base component">
    <VariantSelectContextProvider variants={ProductDetailImageFixture.variantOptions}>
      <ProductDetailContextProvider product={ProductDetailHeaderFixture.product}>
        <ProductDetailImage
          className={ProductDetailImageFixture.className}
          id={ProductDetailImageFixture.id}
          theme={ProductDetailImageFixture.theme}
        />
      </ProductDetailContextProvider>
    </VariantSelectContextProvider>
  </Story>
</Preview>

## API

<Props of={ProductDetailImage} />
