import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { VariantSelectContextProvider } from '~/contexts/VariantSelect.context';
import useVariantSelect from '~/customHooks/useVariantSelect';
import ProductDetailImage from './ProductDetailImage';
import ProductDetailImageFixture from './ProductDetailImage.fixture';

<Meta
  title="Components/ProductDetailHeader/ProductDetailImage"
  component={ProductDetailImage}
/>

# ProductDetailImage

<Preview>
  <Story name="Base component">
    <VariantSelectContextProvider
      value={useVariantSelect(ProductDetailImageFixture.variantOptions)}
    >
      <ProductDetailImage
        className={ProductDetailImageFixture.className}
        id={ProductDetailImageFixture.id}
        onVariantChange={ProductDetailImageFixture.onVariantChange}
        selectedOption={ProductDetailImageFixture.selectedOption}
        theme={ProductDetailImageFixture.theme}
        variantOptions={ProductDetailImageFixture.variantOptions}
      />
    </VariantSelectContextProvider>
  </Story>
</Preview>

## API

<Props of={ProductDetailImage} />
