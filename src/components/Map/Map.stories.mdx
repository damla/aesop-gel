import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Map from './Map';
import MapFixture from './Map.fixture';

<Meta title="Components/Map" component={Map} />

# Map

Presented as with `customPinMarker`.

<Preview>
  <Story name="Base component">
    <Map
      center={knobs.object('center', MapFixture.customPinMarkerCenter, 'Presentation')}
      copy={knobs.object('copy', MapFixture.copy, 'Content')}
      customMarker={knobs.object('customMarker', MapFixture.customPinMarker, 'Content')}
      hasMarkerIndexes={knobs.boolean('hasMarkerIndexes', false, 'Presentation')}
      id="google-map"
      initialZoom={knobs.number('initialZoom', MapFixture.initialZoom, 'Presentation')}
      places={knobs.object('places', MapFixture.places, 'Content')}
    />
  </Story>
</Preview>

## API

<Props of={Map} />

## Variations

### With Place Marker Indexes

<Preview>
  <Story name="With Place Marker Indexes">
    <Map
      center={MapFixture.center}
      hasMarkerIndexes={true}
      id="google-map-indexes"
      places={MapFixture.places}
    />
  </Story>
</Preview>

### With Custom Place Marker

<Preview>
  <Story name="With Custom Place Marker">
    <Map
      center={MapFixture.customPlaceMarkerCenter}
      customMarker={MapFixture.customPlaceMarker}
      hasMarkerIndexes={false}
      id="google-map-custom-place"
      initialZoom={15}
      places={MapFixture.places}
    />
  </Story>
</Preview>
