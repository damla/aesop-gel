import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import * as knobs from '@storybook/addon-knobs';
import KitList from './KitList';
import KitListFixture from './KitList.fixture';

<Meta title="Components/KitList" component={KitList} decorators={[withKnobs]} />

# KitList

<Preview>
  <Story name="Base component">
    <KitList
      isVisible={knobs.boolean('isActive', true)}
      items={KitListFixture.items}
      theme={knobs.select('theme', ['dark', 'light'], 'dark')}
    />
  </Story>
</Preview>

## API

<Props of={KitList} />
