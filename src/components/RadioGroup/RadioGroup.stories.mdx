import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import { State } from '~.storybook/State';
import RadioGroup from './RadioGroup';
import RadioGroupFixture from './RadioGroup.fixture';

<Meta
  title="Components/RadioGroup"
  component={RadioGroup}
/>

# RadioGroup

<Preview>
  <Story name="Base component">
    <State
      initialState={{
        value: '',
      }}
     >
      {(state, setState) => {
        return (
          <RadioGroup
            name={knobs.text('name', 'radioGroup', 'Content')}
            onChange={(event) => {
              event.persist();
              const {
                target: { value },
              } = event;
              setState({...state, value})
            }}
            options={knobs.object('options', RadioGroupFixture.options, 'Content')}
            value={state.value}
          />
        )
      }}
    </State>
  </Story>
</Preview>

## API

<Props of={RadioGroup} />
