import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import List from './List';
import ListFixture from './List.fixture';

<Meta title="Components/List" component={List} />

# List

<Preview>
  <Story name="Base component">
    <List
      items={knobs.object('items', ListFixture.items, 'Copy')}
      theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
    />
  </Story>
</Preview>

## API

<Props of={List} />
