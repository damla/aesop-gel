import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import TwoColumnList from './TwoColumnList';
import TwoColumnListFixture from './TwoColumnList.fixture';

<Meta title="Components/TwoColumnList" component={TwoColumnList} />

# TwoColumnList

<Preview>
  <Story name="Base component">
    <TwoColumnList
      leftColumn={knobs.object(
        'leftColumn',
        TwoColumnListFixture.leftColumn,
        'Content',
      )}
      rightColumn={knobs.object(
        'rightColumn',
        TwoColumnListFixture.rightColumn,
        'Content',
      )}
      theme={knobs.select(
        'TwoColumnList theme',
        ['dark', 'light'],
        'dark',
        'Presentation',
      )}
    />
  </Story>
</Preview>

## API

<Props of={TwoColumnList} />
