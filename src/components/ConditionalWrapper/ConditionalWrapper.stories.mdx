import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import ConditionalWrapper from './ConditionalWrapper';
import ConditionalWrapperFixture from './ConditionalWrapper.fixture';

<Meta title="Components/ConditionalWrapper" component={ConditionalWrapper} />

# ConditionalWrapper

<Preview>
  <Story name="Base component">
    <ConditionalWrapper
      condition={true}
      wrapper={children => <div>{children}</div>}
    >
      <span>Conditionally wrapped content</span>
    </ConditionalWrapper>
  </Story>
</Preview>

## API

<Props of={ConditionalWrapper} />
