import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Quote from './Quote';
import QuoteFixture from './Quote.fixture';

<Meta title="Components/Quote" component={Quote} />

# Quote

<Preview>
  <Story name="Base component">
    <Quote
      author={knobs.text('author', QuoteFixture.author, 'Copy')}
      content={knobs.text('children', QuoteFixture.content, 'Copy')}
      theme={knobs.select('theme', ['dark', 'light'], 'dark', 'Presentation')}
    />
  </Story>
</Preview>

## API

<Props of={Quote} />
