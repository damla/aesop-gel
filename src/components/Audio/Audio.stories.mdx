import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as knobs from '@storybook/addon-knobs';
import Audio from './Audio';
import AudioFixture from './Audio.fixture';

<Meta title="Components/Audio" component={Audio} />

# Audio

<Preview>
  <Story name="Base component">
    <Audio
      artistName={knobs.text('artistName', AudioFixture.artistName, 'Content')}
      audioUrl={knobs.text('audioUrl', AudioFixture.audioUrl, 'Content')}
      copy={knobs.object('copy', AudioFixture.copy, 'Content')}
      hasAutoPlay={knobs.boolean(
        'hasAutoPlay',
        AudioFixture.hasAutoPlay,
        'Presentation',
      )}
      id={AudioFixture.id}
      progressColor={knobs.select(
        'progressColor',
        ['orange', 'green', 'blue'],
        AudioFixture.progressColor,
        'Presentation',
      )}
      trackTitle={knobs.text('trackTitle', AudioFixture.trackTitle, 'Content')}
    />
  </Story>
</Preview>

## API

<Props of={Audio} />
