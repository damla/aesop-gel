name: Test
on:
  push

jobs:
  lint_and_test:
    name: 'Lint, Test, Publish!'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install
        run: npm ci

      # - name: Lint
      #   run: npm run lint

      # - name: Test
      #   run: npm run test

      - name: Publish!
        run: npm publish
